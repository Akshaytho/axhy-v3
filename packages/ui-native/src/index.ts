/**
 * @axhy/ui-native
 *
 * RN Reusables components customized for Axhy brand. Mirrors the web
 * component API where it makes sense, native where it doesn't.
 *
 * @derives(ADR-0016)
 */

export const PACKAGE_NAME = '@axhy/ui-native' as const;
