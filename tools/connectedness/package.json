{
  "name": "@axhy-tools/connectedness",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./src/build.ts",
  "scripts": {
    "connectedness:build": "tsx src/build.ts",
    "connectedness:impact": "tsx src/impact.ts",
    "connectedness:check": "tsx src/check.ts",
    "build": "tsc -b",
    "lint": "echo 'lint placeholder'",
    "typecheck": "tsc --noEmit",
    "test": "echo 'test placeholder'",
    "clean": "rm -rf dist .turbo *.tsbuildinfo"
  },
  "dependencies": {
    "@axhy/knowledge-graph": "workspace:*",
    "yaml": "^2.6.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
