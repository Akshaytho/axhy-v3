/**
 * tools/seed-data
 *
 * Seeds the persistent `axhy-sandbox` tenant on Railway with realistic data:
 * Mr. Reddy's Surya Cleaning, 80 workers, 6 supervisors, 12 sites, recent visits.
 * Used by integration tests and demo flows.
 *
 * Real implementation lands during build phase 1.
 */

console.log('[seed-data] scaffold; populates axhy-sandbox once schema exists');
