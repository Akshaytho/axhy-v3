/**
 * tools/graph-builder
 *
 * Walks the repo, parses Prisma schema + XState machines + Zod schemas + routes
 * + ADRs + workflow files + journey docs, builds all three graphs in Postgres.
 * Runs on every CI commit.
 *
 * Real implementation lands Day 7 of evidence sprint.
 *
 * @derives(ADR-0002)
 */

console.log('[graph-builder] scaffold; real implementation Day 7');
