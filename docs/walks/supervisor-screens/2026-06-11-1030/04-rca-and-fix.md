# 04 — RCA clusters and root fixes

**Clustered at:** 2026-06-11 20:35 IST

## Clusters (10 bugs → 4 roots)

| Cluster | Bugs included (#) | Common root cause (file:line / function / assumption)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Why one root explains them all                                                                                                               |
| ------- | ----------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------- |
| C-A2    | 1, 2              | Same root as worker-walk C-A: screens built + backend-backed but never linked (`_layout.tsx:157-158` href:null, zero nav refs). Secondary surfaces ship without a navigation owner.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Both orphans are the identical pattern, third+fourth instance after worker history/leave.                                                    |
| C-C     | 6, 7              | Client-side reversal-window logic re-implements a SERVER decision with timezone-naive time math (UTC timestamp parsed as local → +5h30m skew on IST devices), then branches the UI on its own wrong answer. The server already owns the truth.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | One wrong client computation produces both the false "window closed" AND the dead-end (the client never offers the path the server wants).   |
| C-D     | 8, 9, 10          | The chat→decision→apply pipeline has no single contract owner — RE-ROOTED after curl repro: (a) propose/apply AUTHZ ASYMMETRY — chat.ts's propose_mark_absent writes the decision with NO roster precheck while markAbsentService refuses unrostered workers (assertCallerSupervisesWorker → NO_PRIMARY_SITE → NOT_SUPERVISOR 400, attendance-service.ts:87-97), AND the AI reply claims "Marked … absent" (done-claim for a refusable proposal); the card then hides the server reason behind raw "status 400". [First theory — payload envelope drift — DISPROVEN: writer stores payload FLAT, supervisor-decision-writer.ts:170]; (b) extractor's success path never invalidates the mobile decisions cache and the tab has no focus refetch → invisible; (c) the queue composer never resolves workerName from the payload workerId → blank cards. | Three symptoms, one root: each pipeline stage assumes another stage handles the contract (authz precheck, cache freshness, display context). |
| C-E     | 3, 4, 5 (+O1,O2)  | No "every word is real" gate on supervisor-surface copy: a dev placeholder ("inline thumbnails ship with…"), an unbacked promise ("notifies HR"), raw audit kinds ("Dwi expired."), US dates, phantom video.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | All are words shown to a user that the system does not back with behavior — the founder's word-truth rule applied to one surface.            |

## Sibling check (founder rule: if one is broken, its siblings are broken)

| Root | Siblings checked                                                                                                                                                                                                                                                           | Result                                                                                                       |
| ---- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------ |
| C-C  | Other client-side relative-time math: decision card "PENDING 17 MIN AGO" (O3) — correct; worker timer screen (worker walk) — correct.                                                                                                                                      | Fault is LOCAL to the reversal-window helper, not a shared util.                                             |
| C-D  | The OTHER chat-writeable kind (propose_leave): checked code — leave apply path has its own authz; the roster-precheck asymmetry pattern must be checked for it at fix time. Dismiss path: row id only — works. Leave/swap queue rows use domain apply routes — unaffected. | propose_mark_absent confirmed broken for unrostered workers; propose_leave precheck verified during the fix. |
| C-E  | Worker-surface copy was walked 2026-06-10 (worker walk) — flagged-visit wording already softened (O7 fix); no dev placeholders found there.                                                                                                                                | Supervisor surface was the un-walked copy surface; this walk covered it.                                     |
| C-A2 | All remaining `href:null` screens in both layouts: worker layout — history (FIXED, F2), leave hidden screens none; supervisor layout — memory/sites/replacement-picker all reachable via drawer/action-sheets.                                                             | summary + updates are the only remaining orphans.                                                            |

## Batch fix plan (roots only — no symptom patches, no refactors)

| Root   | Minimal fix                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Files touched                                                           | Fixed at (IST) | Commit |
| ------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------- | -------------- | ------ |
| C-D(a) | Two-sided: (i) propose-time roster precheck in chat.ts's propose_mark_absent branch — run assertCallerSupervisesWorker BEFORE writing the decision; on failure the AI answers honestly ("X isn't on your site roster — ask HR to assign them first") and writes NO decision; (ii) mobile decision card surfaces the server's error code as plain words (NOT_SUPERVISOR → "This worker isn't on your site's roster…") instead of raw "status 400". Real-DB test for (i). | apps/backend/src/routes/chat.ts; apps/mobile DecisionCard error mapping | (pending)      |        |
| C-D(b) | DONE 21:15 IST `b225a50` — chat invalidates on decision-carrying responses; Decisions invalidates on tab focus. LIVE-PROVEN: badge+list updated without restart (evidence/14,15). Chat surface invalidates `DECISIONS_QUERY_KEY` whenever a response adds decisions; Decisions screen refetches on tab focus.                                                                                                                                                           | apps/mobile (chat decision hook + decisions.tsx)                        | (pending)      |        |
| C-D(c) | DONE 21:15 IST `b225a50` (deploy-pending on prod) — decisions-service resolves workerName for rows whose payload carries workerId (single batched Worker lookup); 15/15 real-DB on the queue route suite. Also C-D(a) both sides in the same commit: propose-time roster precheck in chat.ts (9/9 chat-apply suites) + DecisionCard humanized error codes (LIVE-PROVEN, evidence/13).                                                                                   | apps/backend/src/lib/services/decisions-service.ts                      | (pending)      |        |
| C-C    | Delete the client-side window computation as the BRANCH AUTHORITY: always attempt direct reverse first; if the server answers WINDOW_CLOSED, show the HR sheet. Server error copy de-jargoned ("soft-flag" → plain words).                                                                                                                                                                                                                                              | apps/mobile activity reverse flow; apps/backend error message           | (pending)      |        |
| C-E    | Word-truth pass on the walked copy: photos card stops promising what it can't show (until thumbnails ship, show count + honest "Photos visible to HR on the computer" only if true / else drop); reject sheet drops "notifies HR" until a real notification exists; DWI_EXPIRED humanized ("A pending decision expired unanswered (48h)"); IST date format on review sheet.                                                                                             | apps/mobile flagged-review sheet + activity humanizer strings           | (pending)      |        |
| C-A2   | Placement decision (live phase saw both screens' content): defer wiring until founder picks placement — recorded in 06-verdict as the one open product decision. NOT wired silently.                                                                                                                                                                                                                                                                                    | (decision for founder)                                                  | —              | —      |

### C-C — scouting findings (2026-06-12 00:35 IST, pre-fix, recorded for session handover)

- **Root confirmed at:** `apps/mobile/app/(supervisor)/activity.tsx:111-112` — `isWithinReverseWindow` does `Date.now() - new Date(isoWhen)`; the modal routing at `:962` and the drawer grey-out at `:204` both branch on it as the authority.
- **NEW SIBLING DEAD-END (found scouting, worse than the walked bug):** `softFlagActivity` (activity-reverse-service.ts:471-474) rejects `WINDOW_OPEN` for **ALL kinds — no kind check**. A NON-reversible action inside 30 minutes has NO path at all: Reverse → 422 KIND_NOT_REVERSIBLE, Send-to-HR → 422 WINDOW_OPEN. The service's own comment (≈:443: "the route allows soft-flag from inside the window TOO") claims the opposite of what the code does — doc-drift lie, same family as the audit-caught route headers.
- **Error transport is ready:** mobile `apiFetch` throws `ApiError{status, code}` with `code` = the server's `error` field (`WINDOW_CLOSED`/`WINDOW_OPEN`/`KIND_NOT_REVERSIBLE`); mutations in `apps/mobile/lib/queries/use-activity-reverse.ts`. No new wire format needed.
- **Fix design (execute next session):**
  1. Mobile: delete `isWithinReverseWindow`; route modals on KIND only (reversible → typed-phrase ReverseConfirmModal, non-reversible → HR sheet); drawer Reverse never time-greyed; on reverse error `WINDOW_CLOSED` → auto-open the HR sheet for the same row; HR-sheet copy parameterized by arrival reason (window-closed vs needs-HR-kind) so it never claims "window closed" when the reason is the kind.
  2. Backend: `softFlagActivity` returns WINDOW_OPEN only when the kind IS reversible (non-reversible kinds may go to HR anytime — it is their only path); fix the lying comment.
  3. Backend copy: reverse route :94 → "The 30-minute undo window has passed. Send it to HR for review instead."; soft-flag route :196 → "This can still be undone directly — use Reverse."
  4. Tests: real-DB cases for (non-reversible kind, in-window soft-flag → OK) and (reversible kind, in-window soft-flag → WINDOW_OPEN); then live emulator re-walk of the REVERSE bad-day.

## RESOLUTION — 2026-06-12 (C-C + C-E done)

**C-C — server is now the sole reverse-window authority.**

- **Backend** (`activity-reverse-service.ts`): `softFlagActivity` rejects `WINDOW_OPEN` only when `isReversibleActivityKind(source.kind)` — a non-reversible kind is accepted at any time (HR is its only path), closing the both-walls-422 dead end. The lying doc-comment (≈:443) is replaced with the accurate kind-aware rule. Route copy de-jargoned: reverse `WINDOW_CLOSED` → "The 30-minute undo window has passed. Send it to HR for review instead."; soft-flag `WINDOW_OPEN` → "This can still be undone directly — use Reverse." **Proven:** new real-DB case in `test/activity-reverse-regression.test.ts` (non-reversible `SITE_COMPLAINT_LOGGED` in-window: `/reverse`→422 `KIND_NOT_REVERSIBLE` AND, before the fix, `/soft-flag`→422 `WINDOW_OPEN`; after the fix `/soft-flag`→200 with a `LATE_REVERSAL_REQUEST` row). Suite 1/1 green against `DATABASE_PUBLIC_URL`; §8 reversible-in-window→`WINDOW_OPEN` still asserted (no regression). Decision check: the 2026-05-18 sprint-2 audit called the `WINDOW_OPEN` rejection "intentional," but its reasoning assumed `/reverse` is the alternative — which only exists for reversible kinds; the fix preserves that intent and only fills the non-reversible gap.
- **Mobile** (`app/(supervisor)/activity.tsx`): deleted `isWithinReverseWindow` (the timezone-naive `Date.now()-new Date(iso)` math that false-closed the window on IST devices). Reverse routes on KIND only — reversible → `ReverseConfirmModal` (and on a server `WINDOW_CLOSED` it auto-opens the HR sheet for the same row via the `ApiError.code` branch); non-reversible → HR sheet directly. `SoftFlagConfirmModal` copy parameterized by `reason` ('window-closed' vs 'needs-hr') so it never claims "window closed" for a kind that has no window. Drawer Reverse button no longer time-greyed. Stale doc-comments corrected. **Proven live** (re-walk 2026-06-12, evidence/rewalk-2026-06-12): always-active Reverse + the honest "can't be undone directly" HR sheet.

**C-E — word-truth pass.**

- `FlaggedReviewSheet.tsx`: dev note "Inline thumbnails ship with the photo CDN slice." → "{n} photos captured / Not shown in the app yet — judge from the AI's reason below."; false "and notifies HR" → "and records it in the audit trail" (grep-verified: reject path writes only the `VISIT_REJECTED` audit, no outbox/Notification); `when` date → `toLocaleString('en-IN', …)` (was US `m/d/y`).
- `audit-summary.ts`: added `DWI_EXPIRED` case → "A proposed decision expired unanswered after 48 hours." (was the generic humanizer's "Dwi expired." jargon).
- `Drawer.tsx`: "How to use Axhy" sub "60-sec video · examples" → "Quick guide" (the /help page has no video). **Proven live** (evidence/rewalk-2026-06-12/03).

**Deploy-gated:** the backend halves (soft-flag gate, route copy, DWI humanizer) run on prod and activate at the founder's next backend deploy from this branch; they are proven by the real-DB regression suite, not yet by a prod walk. **Live-verification deferred (honest):** FlaggedReviewSheet copy (no flagged visit in QA data) and the reversible-in-window ReverseConfirmModal path (QA worker has no roster) — see 05-rewalk-proof.md.

**C-A2** (orphan `summary`/`updates` screens) remains the one open product decision for the founder (placement) — unchanged from 06-verdict.
