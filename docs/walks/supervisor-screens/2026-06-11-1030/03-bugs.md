# 03 — Bugs (collected during the walk; NOTHING fixed mid-walk)

| #      | Found at (IST)                              | Step/screen              | Persona    | What happened vs what should                                                                                                                                                                                                           | Evidence (screenshot + DB query + file:line)                                                                              | Severity                              | Feeling impact                                                                                     |
| ------ | ------------------------------------------- | ------------------------ | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------- | ------------------------------------- | -------------------------------------------------------------------------------------------------- |
| 1      | 2026-06-11 10:30 IST (code-traced, Step 1b) | summary (supervisor)     | Supervisor | summary.tsx exists and its backend supervisor-summary.ts returns a real performance digest, but the screen is hidden (`_layout.tsx:157` href:null) with ZERO navigation to it anywhere → a supervisor can never open their own summary | `app/(supervisor)/_layout.tsx:157`; zero push/href/pathname/Link across app/(supervisor)+components; backend route exists | MAJOR (whole screen invisible)        | The supervisor's at-a-glance performance view is built but unreachable — they don't know it exists |
| 2      | 2026-06-11 10:30 IST (code-traced, Step 1b) | updates (supervisor)     | Supervisor | updates.tsx (HR-updates digest) is hidden (`_layout.tsx:158` href:null), zero navigation; backend supervisor-updates.ts EXISTS → HR notifications to the supervisor have no surface to land on                                         | `app/(supervisor)/_layout.tsx:158`; zero nav refs; backend route exists                                                   | MAJOR (HR→supervisor comms invisible) | HR sends the supervisor an update; the supervisor has nowhere to see it — silent broken channel    |
| (twin) | 2026-06-11 10:30 IST                        | Drawer "How to use Axhy" | Supervisor | Same broken `axhy.app/help` link as worker bug #3 — opens the admin login wall. ALREADY FIXED this session by the F3 /help page (covers both drawers); activates at the next admin-web deploy.                                         | `components/Drawer.tsx:289`; F3 page apps/admin-web/app/help/page.tsx                                                     | (fixed)                               | sibling win — one fix covered both personas                                                        |

**Live-phase note:** bugs 1+2 cluster with the worker walk's RCA C-A ("promised-but-unwired secondary surfaces") — same root, now spanning both personas. Placement decisions (where to surface summary/updates) deferred to the live phase so they're chosen from what the screens actually render.

Severity: BLOCKER (user stuck / data wrong) · MAJOR (works but wrong/scary) · MINOR (friction/ugly) · TRUST (screen lied).
"Feeling impact" = what it does to the person (fear about pay, confusion, distrust) — founder-mandated column.
