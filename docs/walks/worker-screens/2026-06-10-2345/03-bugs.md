# 03 — Bugs (collected during the walk; NOTHING fixed mid-walk)

| #   | Found at (IST)                              | Step/screen                        | Persona | What happened vs what should                                                                                                                                                                                               | Evidence (screenshot + DB query + file:line)                                                                                                                                                                                                                                                                   | Severity                                           | Feeling impact                                                                                                                                                                                                                             |
| --- | ------------------------------------------- | ---------------------------------- | ------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | 2026-06-10 23:50 IST (code-traced, Step 1b) | History (tabs/history.tsx)         | Worker  | History screen + working backend route `/worker/history` exist, but the tab is hidden (`href: null`) and NO button/link anywhere navigates to it → a worker can NEVER see their past visits                                | `app/(worker)/(tabs)/_layout.tsx:96`; zero nav hits in grep across app/(worker) + components/worker; NAV_ROUTES has no history entry (lib/api-routes.ts:54-67); **LIVE-CONFIRMED 2026-06-11 00:39 IST** — tab bar shows only Today/Capture/You (walk_19 screenshot)                                            | MAJOR (whole sub-feature invisible)                | A worker who wants proof of yesterday's finished work — "did it count for my pay?" — has nowhere to look; the reassurance feature exists in code but not in his hands                                                                      |
| 2   | 2026-06-11 00:49 IST (live walk, Step 20)   | Leave success card → Profile (You) | Worker  | Success card says verbatim "Your supervisor will review it. **You'll see the result on your profile.**" — but the profile screen has NO leave section: no pending request, no status, nothing. The promise is false today. | walk_28 screenshot (promise) vs walk_29 screenshot (profile with no leave UI); DB row exists and is correct (`REQUESTED 2026-06-12 Family functiony`) so the DATA is fine — only the promised surface is missing. Check `app/(worker)/(tabs)/profile.tsx` for absent leave section (possible ghost-UI sibling) | TRUST (screen lied about where to find the result) | Suresh filed leave for a family function. Tomorrow he checks his profile: nothing. Did it fail? Should he file again (duplicates)? Should he risk asking the supervisor? The exact pay/permission anxiety this feature was meant to remove |

| 3 | 2026-06-11 01:01 IST (live walk, drawer) | Help & support (WorkerDrawer) | Worker | Tapping "Help & support" opens `https://axhy.app/help` in the browser — which **redirects to the admin-web LOGIN page** (`/login`). No help page exists. The worker who needs help lands at a locked admin door asking for credentials he doesn't have. On-device it rendered as a blank tab. | `components/worker/WorkerDrawer.tsx:121` (`Linking.openURL('https://axhy.app/help')`); `curl -L https://axhy.app/help` → HTTP 200, final URL `https://axhy.app/login` (2026-06-11 01:03 IST); walk_32 screenshot (blank Chrome tab); admin-web page inventory has no /help route | MAJOR + TRUST (button promises help, delivers a locked door) | Suresh is stuck or confused, taps the one button named "help" — and gets a login wall in English for a portal that isn't his. He learns the help button is useless and stops trusting the menu |

| 4 | 2026-06-11 01:06 IST (live walk, Step 21) | Sign out | Worker | Sign-out works (DB: RefreshToken revokedReason=LOGOUT) but throws an unhandled promise rejection: `Error: Unable to activate keep awake` — expo-keep-awake activation racing the sign-out unmount. Dev builds show a scary red toast; prod builds swallow it silently (unmonitored — no Sentry). | walk_33 screenshot (toast); `adb logcat`: "Unable to activate keep awake"; keep-awake is used on capture/timer screens per standing rule — activation appears to fire on a screen that unmounts during logout. DIAGNOSIS 01:55 IST: attribution UNCERTAIN — the only product keep-awake call sites are timer.tsx:56-64 (guarded, .catch) and PhasePhotoCapture.tsx:74 (bare useKeepAwake(), NO catch), and NEITHER mounts on the sign-out path → the toast may be expo-dev-client noise (dev builds only). Sibling inconsistency is real regardless: PhasePhotoCapture:74 unguarded vs timer.tsx:61 guarded. Re-test on the capture walk where these actually mount before fixing | MINOR (cosmetic in prod today, but an unhandled rejection is a latent crash risk on RN upgrades) | Suresh sees a red error box at the exact moment he expects a clean goodbye — "did I break something?" |

| 5 | 2026-06-11 01:18 IST (live walk, negative test) | OTP entry — wrong code | Worker | Entered wrong code `999999` → app silently bounced ALL the way back to the phone-entry screen with NO error message. Expected: stay on OTP screen with "Wrong code…" (that exact handler EXISTS at otp.tsx:98-99 but never runs). Worker re-requests OTP, burning the 3-per-15-min quota → third mistake = 15-minute lockout at shift start. | walk_36 screenshot (phone screen, no error); **ROOT: `lib/api.ts:283-285`** — blanket `if (res.status === 401) await handleUnauthorized()` lacks the `/auth/*`+`auth:false` exemption that line 261 has, so the wrong-code 401 triggers wipe+navigate before otp.tsx's catch can render | MAJOR (silent reset + quota burn → real lockouts) | Suresh fat-fingers ONE digit at 6 AM → thrown to the start with zero explanation → tries again, again — locked out 15 min while his shift starts. He doesn't know why. The app feels broken at the worst moment |

**Observations (not bugs, for verdict/RCA context):**

- O1 (00:43 IST): Profile masks the worker's OWN phone fully ("+91 ••••• •••••") — he can't confirm which account he's on; last-4 visible would be kinder.
- O2 (00:43 IST): Display name is generic "Worker" — seeded data, but check fallback behavior when User.name is null.
- O3 (00:45 IST): IST date handling CORRECT on home + leave screens at 00:39-00:45 IST (inside the UTC danger window) — the C1 UTC-default bug did NOT reproduce on these screens (it lives in chat mark-absent defaults, different surface).
- O4 (00:37 IST): Dev-build toasts covered the permissions Continue button — dev-only; verify release build has no equivalent overlay.
- O5 (01:27 IST): Permissions + consent screens re-shown on EVERY fresh login (API dedups consent via 409; permission grants remembered with honest "Granted ✓" but the screen still interposes). Legally conservative; adds two taps to every re-login — consider skip-if-already-done routing.
- O6 (01:29 IST): Offline-error copy "Network request failed" is engineer-speak; "No internet — check your connection and try again" fits the worker better.
- O7 (03:07 IST): Worker-facing flagged screen shows the raw AI reasoning incl. "may be staged or improperly uploaded" — accurate but reads as an accusation to a worker; consider a softer worker-facing line ("We couldn't see your work clearly — your supervisor will check it") while supervisors keep the full reasoning.
- BUG #4 RE-ATTRIBUTION (03:08 IST): entire capture phase ran with BOTH keep-awake call sites mounted — zero error toasts. walk_33's sign-out toast = expo-dev-client noise (dev builds only), NOT product code. Bug #4 downgraded to hygiene: standardize PhasePhotoCapture.tsx:74 to the guarded pattern.

Severity: BLOCKER (user stuck / data wrong) · MAJOR (works but wrong/scary) · MINOR (friction/ugly) · TRUST (screen lied).
"Feeling impact" = what it does to the person (fear about pay, confusion, distrust) — founder-mandated column.
