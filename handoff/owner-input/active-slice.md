# Active Slice

> Exactly one slice in flight at any time. This file is the single source of truth for the dashboard's "Current slice focus" callout AND the page-header active-slice banner (friend's 2026-05-15 evening reconciliation).

## Current

| Field                                       | Value                                                                                                                                                                                                                                                                                      |
| ------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **Slice name**                              | `chat-writes-proposed-decisions` (F-002 — round-2 remediation, R2b-iii approved 2026-05-16)                                                                                                                                                                                                |
| **Status**                                  | `CHANGES_REQUESTED` → round-2 plan APPROVED 2026-05-16 → implementing R1 + R2a + R2b-iii + R3. Will move to AWAITING_APPROVAL once all 4 fixes land + tests green + control surface clean.                                                                                                 |
| **Branch**                                  | `feat/layer-1-core-primitives`                                                                                                                                                                                                                                                             |
| **Last landed commit**                      | `f06c98a` — `docs(handoff): F-002 round-2 plan revised — commit to R2b-iii (refactor)`                                                                                                                                                                                                     |
| **Pre-remediation slice commits**           | `a8b4e79` · `73ee9eb` · `8d20db0` · `7fbddcb` · `12f27ed` · `662e146` (round-1 implementation; kept in tree)                                                                                                                                                                               |
| **Round-1 remediation commits**             | `f2b2d74` · `ec01f62` · `38b9987` · `2e03315` · `4506b3d` · `2557e1f` (registry + CHECK + race-safe writer + apply-after-domain + required decisionId + read-side registry + tests; landed but the apply-after-domain trade-off in F-002.4 is **superseded** by the round-2 R2b-iii plan). |
| **Round-2 remediation (planned)**           | F-002.9 termination tx reorder · F-002.10 auth re-check in commitApply · F-002.11 route-level tests · F-002.12 + F-002.13 + F-002.14 extract 4 services · F-002.15 chat refactor to true atomicity · F-002.16 atomicity tests · F-002.17 tracker.                                          |
| **Workflow IDs affected**                   | `D17` · `D20` · `C11` · `E21` · `E22` · `E24`                                                                                                                                                                                                                                              |
| **Current tests status (round-1 sweep)**    | 11/11 test files green · 61/61 cases pass on fresh local Postgres 16 with 12 migrations. New round-2 tests in F-002.11 + F-002.16 will add ~10 more cases.                                                                                                                                 |
| **Friend's required cleanup (this commit)** | Remove superseded "apply-after-domain trade-off" wording so the control surface tells one truth: R2b-iii is the path, the orphan-domain-effect leak is no longer accepted.                                                                                                                 |

## What R1 + R2a + R2b-iii + R3 will deliver (vs the 3 round-2 findings G1/G2/G3)

| Finding          | Concern                                                                                                                                                                                                                                                | Resolution                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| G1               | Termination tx commits `appliedAt` BEFORE worker validation; early-return sentinel let the tx commit with partial state.                                                                                                                               | **R1** — reorder. Validate worker FIRST; lifecycle SECOND; worker.update THIRD. Throw on validation failure so tx rolls back. Lesson L1 applied (memory entry: tx-callback early-return commits partial state).                                                                                                                                                                                                                                                                                     |
| G2               | `commitApply` did not re-check authorization; stale-auth window between preCheckApply (tx 1) and commitApply (tx 2) let DWI_APPLIED record under the wrong actor.                                                                                      | **R2a** — re-check `isCallerAuthorized` inside `commitApply` before the conditional UPDATE.                                                                                                                                                                                                                                                                                                                                                                                                         |
| G2 (domain side) | Under apply-after-domain (F-002.4), the inject's domain effect could land BEFORE commitApply, then commitApply could reject (stale auth, concurrent dismiss). Domain effect under stale authority is real-world behaviour drift, not just audit drift. | **R2b-iii (SUPERSEDES F-002.4's apply-after-domain trade-off)** — extract the 4 inject-style routes into tx-callable service functions. `/chat/apply` calls preCheckApply + service + commitApply INSIDE one `withTenantContext(prisma, companyId, async (tx) => ...)`. All three commit together or none commits. No `app.inject` in the apply path for these branches. Lesson L3 applied (memory entry: "document + reconcile via audit" too weak when side effect can corrupt real-world state). |
| G3               | Round-1 concurrency tests called the writer directly; the full route flow (with the domain-effect-side hole) was not under test.                                                                                                                       | **R3** — new route-level concurrency + stale-auth tests using `app.inject` (and the new direct-call apply once R2b-iii lands).                                                                                                                                                                                                                                                                                                                                                                      |

## P10 failure matrix (planned, post-R1 + R2a + R2b-iii + R3)

| Question                                                     | Answer                                                                                                                                                                                                                                                                                                                             |
| ------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| What invariants does this slice introduce?                   | (1) PROPOSED transitions to exactly one terminal state. (2) `appliedAt` + `dismissedAt` mutually exclusive. (3) Transitions require currently-responsible (binding-routable) OR origin supervisor (origin-only). (4) Domain effect + lifecycle commit happen in ONE Prisma transaction — neither succeeds alone.                   |
| How is each invariant enforced?                              | (1) Conditional updateMany on every transition. (2) DB CHECK constraint (migration 20260518) + conditional WHERE. (3) `isCallerAuthorized` via DECISION_KIND_REGISTRY.routingMode at BOTH preCheck AND commit. (4) `/chat/apply` for all 5 branches wraps preCheckApply + domain-service + commitApply in one `withTenantContext`. |
| What happens on failure of the domain effect?                | Tx rolls back. Lifecycle stays PROPOSED. No DWI_APPLIED audit. Caller sees the domain's error code. No partial state on disk.                                                                                                                                                                                                      |
| What happens on stale authority between preCheck and commit? | R2a's re-check throws NOT_RESPONSIBLE inside commitApply — but because preCheck + service + commit are all in ONE tx, the throw rolls back the domain service call too. NO domain effect. NO DWI_APPLIED.                                                                                                                          |
| What happens for retry / double-submit?                      | Conditional updateMany returns count=0 → discriminator → ALREADY_APPLIED. Domain idempotency is each domain route's own concern (unchanged).                                                                                                                                                                                       |
| What happens under concurrent requests on the same row?      | PG row-lock guarantees exactly one of N concurrent UPDATEs wins. DB CHECK rejects the impossible state. Audit reflects only the winner.                                                                                                                                                                                            |
| What happens for a stale client (old shape)?                 | 400 BAD_INPUT with `decisionId is required`. Row unaffected. (Tested in `chat-apply-transitions-decision.test.ts`.)                                                                                                                                                                                                                |
| What is still intentionally deferred (with sunset)?          | Full state ENUM column (FAILED / EXPIRED / UNDONE) — needs concrete triggers (cron sweep etc.) in their own slices. **No corruption windows remain for the PROPOSED → APPLIED/DISMISSED transitions this slice covers.**                                                                                                           |

## Research sources cited per Rule P9

- [Prisma interactive transactions](https://www.prisma.io/docs/orm/prisma-client/queries/transactions) — conclusive: "all queries inside it have to be run on the same connection." Confirms `app.inject` cannot share a Prisma tx; R2b-iii (refactor) is required, not pessimistic locking.
- [Prisma `updateMany`](https://www.prisma.io/docs/orm/reference/prisma-client-reference#updatemany) — returns `BatchPayload { count }`; count=0 ↔ no rows matched WHERE.
- [PostgreSQL transaction isolation](https://www.postgresql.org/docs/current/transaction-iso.html) — row-locking + WHERE re-evaluation guarantee exactly-one-wins for conditional UPDATE.
- [PostgreSQL CHECK constraints](https://www.postgresql.org/docs/current/ddl-constraints.html) — per-row, evaluated at UPDATE time.
- [PostgreSQL explicit locking](https://www.postgresql.org/docs/current/explicit-locking.html) — surveyed; not needed under R2b-iii because nothing spans a non-DB operation.

## Hash-truth convention

The hash columns above name ONLY landed commit hashes. Under the auto-regen pre-commit hook the new commit's hash is created AFTER the file is written and staged, so at write-time we cannot know the hash that will contain this file. Convention:

- List only commits already in `git log`.
- After a commit lands, the NEXT edit to this file names that commit explicitly.
- No "landing now", no "may land", no "next commit will be", no "in this commit" wording.

## How to read this file

- HTML dashboard auto-renders this content at the top of every page.
- Header active-slice banner + Current Slice focus callout both read from here (single source).
- Source of truth = this markdown. Generated HTML is derivative.
- Update this file at every state transition (rule 19).
